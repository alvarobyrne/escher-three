{
  "name": "escher-three",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "parcel index.html",
    "build": "parcel build index.html",
    "codemirror": "parcel codemirror.html",
    "deploy4win": "rmdir dist /s /q && npm build && gh-pages -d dist",
    "deploy": "rm -rf dist && npm build && gh-pages -d dist"
  },
  "homepage": "https://alvarobyrne.github.io/escher-three/",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gh-pages": "^3.2.3",
    "parcel-bundler": "^1.12.5"
  },
  "dependencies": {
    "codemirror": "^5.61.0",
    "dat.gui": "^0.7.7",
    "hydra-synth": "^1.3.8",
    "three": "^0.127.0"
  }
}
